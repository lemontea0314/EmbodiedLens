# EmbodiedLens 3.0：真实数据发现、修改依据与论文贡献定位

本报告基于用户提供的 VLN-DUET trace 和实际导出代码，分析日期为 2026-09-14 至 2026-09-15。代码已实现以下系统修改；专家研究、受控对照和模型干预实验尚未开展。这里提出的是论文的研究定位与验证方案，不是录用保证，也不是已经证实的新失败理论。

## 1. 最重要的判断

要增强 TVCG/TVC 投稿价值，重点应从“有五个联动图”转向一个可验证的问题：**面对不完整且混合代理指标的导航日志，研究者如何从群体行为定位到具体决策，并形成与证据强度相符的诊断？**

真实数据中最值得解决的是全局动作与局部候选不一致、决策点与实际经过路径不一致，以及代理指标被误读为模型内部证据。3.0 据此重构了数据语义和视觉分析流程。修复这些问题是有效研究的基础；若要成为论文贡献，还需证明这种设计相对合理基线改善了诊断任务。

## 2. 数据范围与可复现统计

来源：VLN-DUET 的本地诊断导出目录。读取 `duet_r2r_val_unseen.json` 和 `duet_r2r_all_splits.jsonl`，并核对 `utils/embodiedlens_trace.py` 与 `r2r/agent.py` 中记录和动作执行逻辑。没有修改这些上游源文件。

| Split | Episodes | Decisions | Scans | SR | SPL | NE (m) | nDTW |
|---|---:|---:|---:|---:|---:|---:|---:|
| val_train_seen | 150 | 918 | 34 | 96.67% | 0.9501 | 0.3106 | 0.9586 |
| val_seen | 1,021 | 6,820 | 56 | 78.84% | 0.7288 | 2.2808 | 0.7874 |
| val_unseen | 2,349 | 16,856 | 11 | 71.52% | 0.6041 | 3.3124 | 0.6702 |
| test | 4,173 | 30,721 | 18 | 不可用 | 不可用 | 不可用 | 不可用 |

all_splits 文件合计 7,693 episodes。val_unseen 单文件是其中同一 split，不应作为额外独立样本重复统计。不同 split 的 scan 数不可直接相加当独立场景数。val_train_seen 只是当前导出的 150 条，不代表完整训练集。以上是当前 trace 的聚合结果，不宣称等于 DUET 论文的完整评测配置。

test 原始导出仍含看似有效的 success、NE 等字段，但本次没有可靠测试 GT 支撑，3.0 默认屏蔽这些 outcome 和 GT 派生诊断。行为轨迹、候选和覆盖分析仍可进行。不能把导出的 4,172 个 Early stop 当作模型测试失败率。

随包携带全部 2,349 条 val_unseen，通过共享 11 份 topology 将 JSON 从 152,092,008 字节降至 34,987,584 字节，没有抽样、删除 episode 或改变候选概率。all_splits 原文件不重复打包；其完整审计结果保存在 `data/analysis-summary.json`，系统支持直接导入原 JSONL。

## 3. 真实数据暴露的问题与修复

以下数值均以 val_unseen 为分母，step 与 episode 分母分别标明。

| 发现 | 为什么影响论文可信度 | 3.0 的修改 |
|---|---|---|
| 5,943/16,856 steps（35.26%）的 local raw p 总和不足 0.99 | 局部候选不是模型完整动作空间；重新归一化会改变原始模型概率 | 保留 raw p，显式编码已观测概率质量和缺失质量 |
| 1,213 steps（7.20%）的真实 chosen 不在局部表 | 用局部 argmax 替代会画出智能体没做的动作 | 以完整 chosen viewpoint ID 定位，补充 G/unlogged 行，概率不补值 |
| 12,295 steps（72.94%）含重复方向标签 | 两个 left/forward 不是同一动作，按名称匹配会错 | 全部链接和选择按 viewpoint ID；方向只作描述 |
| 导出 revisit_pressure 全零，但 550 episodes（23.41%）的完整路径有回访 | 只看决策端点漏掉全局动作经过的中间节点 | 从分段 trajectory 重建完整运动路径，显示每次决策运动段和实际回访 |
| 61 episodes 有停止后的返回路径 | 最后决策位置、停止点距离和最终 NE 不能混为一谈 | 保留完整轨迹，单独标注 post-stop 事件与 stop distance |
| failure_type 只有 Success 1,680 / Early stop 669 | 导出器标签不是经过专家验证的失败机制 | 结果与可并存行为事件分开；不绘制伪多类别故障解释 |
| 图片 0，提供的 rollout 0；grounding/evidence/utility 是 proxy | 无法证实模型看见了什么，更不能以几何路径冒充模型干预 | 移除无依据的 grounding 解释，E 明示 Geometry only，图片/rollout 按来源可用性展示 |
| 旧 clause_index 按时间比例产生 | 时间位置不能作为模型语言注意力 | 展示原始指令，不把估计 clause 高亮当作对齐证据 |

全图 XYZ 加权距离与所有有效 step 原生距离的核对中，没有差异超过 0.05 m 的记录。该结果支持当前拓扑距离计算的数值一致性，不证明指令语义 oracle 正确。

原始概率是真实模型数值；原 candidate 的 proxy 标记不意味着概率也是人工生成。导出器把几何与概率构造的 evidence/utility 混在同一候选记录中，所以必须按字段解释来源。

## 4. 交互设计修改及其研究目的

**A：固定参考集合中的可解释检索。** 默认六维行为/覆盖特征排除 outcome，显示分布和 SR 的事后描述。Representative、Severe、Boundary 回答不同分析意图，显示选中 ID 和分数；过滤后不偷偷重新聚类。严重度和 margin 是明确启发式，不能称自动发现最有价值案例。

**B/C：决策与运动两个尺度的对齐。** B 展示完整实际轨迹与当前决策的运动段；C 同时显示全局 chosen、局部候选排名、概率覆盖、几何冲突、参考分歧、距离变化与物理回访。其目的在于识别“没有重复决策端点，但实际路线回访”的情况。

**D/E：区分观察、参照和假设。** D 保留动作身份与概率；E 的图最短路仅是离线几何参照。系统允许研究者记录假设、固定比较案例、导出 SVG 和包含来源哈希的分析记录，形成可复查的分析过程。

界面采用分页列表、可横向滚动的长序列、共享状态联动和局部拓扑上下文。加载在 Web Worker 中进行，数据和模型计算保留在本地。

## 5. 真实案例与可主张的发现边界

### 3411_1，t8：候选表不能代表完整决策

raw local mass 为 0.5866057747；真实 chosen 是 `c56e92a10dda45a0a27fe34224c8294e`，未在局部表。3.0 将它显示为 G，并在 B 呈现实际经过的运动段。该案例证明旧的局部归一化/argmax 回退会导致错误阅读。它尚不能证明 DUET 的融合模块有缺陷，因为完整 global/local/fused logits 尚未导出。

### 3766_2：完整运动轨迹揭示回访

该例有 15 次重复访问，回访率 0.5；完整路径长 51.66 m，起点到目标图距离约 7.57 m。含 6 次局部表未记录 chosen、11 次局部概率覆盖不足；默认定位最早几何冲突 t3。系统支持追踪“全局选择如何带来较长的重复路径”。判断其为错误记忆、合理探索或指令约束，需要补充模型信号、视觉观察和专家判断。

### 1744_2：停止决策与最终位置不同

此例包含停止后的返回。分析时必须同时查看停止点和完整轨迹终点，不能把停止处的距离替代最终 NE。该现象适合评估用户能否理解策略终止语义。

总体上 1,787 episodes 有至少一次几何冲突，1,873 有参考路线分歧。它们与成功/失败结果不是同义词，不能报告为“错误率”。成功 episode 也可能偏离最短路或参考路径。

## 6. 聚类初步诊断

默认 K=5 时各组 N 为 996、805、295、164、89；SR 分别约 84.14%、65.09%、73.22%、45.12%、31.46%。这些是对当前六维空间的事后描述，尚未校正路线难度、指令长度、场景或日志覆盖的混杂。

| K | 两次初始化 ARI | SSE |
|---|---:|---:|
| 3 | 1.0000 | 4783.77 |
| 4 | 1.0000 | 3713.86 |
| 5 | 0.9543 | 3392.45 |
| 6 | 1.0000 | 2922.35 |
| 7 | 1.0000 | 2737.97 |
| 8 | 0.9943 | 2595.25 |

这里只变两个确定起点。高 ARI 不等于特征合理、无日志偏差、跨场景稳定或跨模型可泛化；SSE 随 K 增加下降也不是选择 K 的充分理由。后续应开展特征消融、更多初始化、按场景/路线重采样、模型/检查点迁移，必要时增加纯行为特征对照。

## 7. 论文可以怎样写贡献

建议标题方向：**EmbodiedLens: Evidence-aware Visual Diagnosis of Global Decisions and Physical Traversals in Vision-and-Language Navigation**。

在完成后续研究后，可围绕以下三项组织贡献：

1. **VLN 不完整日志的任务与证据抽象。** 区分全局决策、局部候选、实际运动、几何参照和代理指标，建立从“能观察什么”到“能主张什么”的诊断任务。需用领域专家需求及跨 trace 例证支撑其一般性。
2. **连接群体、决策与运动路径的视觉分析方法。** 以不同目的的案例检索连接总体分布与 B/C 的双尺度证据，再由 D/E 检查备选解释。需证明这些编码和交互解决现有方法难以完成的任务。
3. **可复现的证据审计与实证研究。** 提供数据语义检查、分析状态记录、来源标识和真实案例，并以定量/定性评价验证其对诊断正确性和效率的影响。

目前第 1 项是基于实际日志的初步抽象，第 2 项已有可运行系统，第 3 项已提供复现材料但尚缺用户评价。不能声称已提出全新聚类算法、已发现因果失败机制或已完成可解释 AI 的忠实度证明。k-means、中心最近样本和 margin 本身不是本项目算法创新。

VLN 诊断已有相关研究，例如 [Diagnosing Vision-and-Language Navigation: What Really Matters](https://aclanthology.org/2022.naacl-main.438/)。需要系统比较既有 VLN 分析、模型调试和不确定性可视化工作，不能仅与自己的旧界面对比后宣称“首次”。[DUET 原论文](https://arxiv.org/abs/2202.11742) 提供全局/局部决策的模型背景。

## 8. 投稿前必须补的评价

[Nested Model](https://www.cs.ubc.ca/labs/imager/tr/2009/NestedModel/) 强调问题、任务抽象、编码/交互和算法层次的验证；[Design Study Methodology](https://www.cs.ubc.ca/labs/imager/tr/2012/dsm/) 可帮助组织领域合作。以下是待执行方案，不是已完成结果。

| 研究问题 | 建议任务与对照 | 应收集的证据 |
|---|---|---|
| RQ1：研究者能否正确理解不完整日志？ | 给定未导出 chosen、方向重名、代理指标案例；对比合理的表格+路径基线与完整系统 | 动作身份/证据可用性判断正确率、误把 proxy 当事实的比例、时间与置信度 |
| RQ2：双尺度表示是否改善路径诊断？ | 定位实际回访、首次几何冲突、停止后返回；对比仅决策点视图与完整运动段视图 | 定位正确率、漏检率、任务时间、带证据的解释质量 |
| RQ3：案例检索是否提高分析覆盖？ | 同一群体中解释典型行为、严重结果和边界差异；比较随机/首条/三种明确选择器 | 多样性、代表性判断、覆盖不同现象的数量；避免将选择器提供不同难度样本混为界面效应 |
| RQ4：专家能否形成可行动的模型假设？ | 真实调试会话，追踪假设提出、替代解释与后续验证 | 专家思维过程、可复现分析链、已验证/被否定假设和设计局限 |

先访谈实际开发/评估 VLN 的研究者，确定任务和术语，再做试验估计效应与功效，决定正式样本量。不要预填参与人数、显著性、提升百分比或专家结论。

定量实验采用相同数据与同等基础功能的对照；分离语义修复带来的收益与可视化编码的收益。可分别消融概率覆盖提示、运动段、自动案例检索；任务顺序与条件顺序平衡，避免学习效应。以专家独立标注和一致性检查建立任务答案，对“失败原因”允许未知/多解释。

R2R 同一路线可对应多个指令，样本不独立。比较模型时尽量按相同 instruction/route 配对，统计时按路线或场景分组，报告效应量与区间；仅 11 个 unseen 场景时应明确推断局限。页面 Wilson 区间只是按 episode 独立假设的描述，不可直接充当显著性证据。

补充至少一个不同模型、检查点或受控策略条件，有助于检验抽象是否只适用于当前 DUET 导出器。性能实验应覆盖更大数据量并报告硬件、浏览器、加载与交互时间分布；本次脚本耗时不能当可发表性能基准。

## 9. 对 TVCG 与 TVC 的具体建议

[TVCG 官方范围](https://www.computer.org/digital-library/journals/tg/cfp-ieee-transactions-on-visualization-computer-graphics) 包括可视分析、系统和评价。以 TVCG 为目标，更应证明任务抽象、证据编码和交互方法的可推广价值，而不是只展示导航指标面板。当前系统有合适的应用问题，但尚需充分相关工作、领域合作与严谨评价。

[The Visual Computer 的范围](https://link.springer.com/journal/371/aims-and-scope) 涵盖视觉计算及相关 AI/可视化问题。选择 TVC 时也需要明确新方法和实验证据，不能假定工程集成更容易被接受。按照其[投稿说明](https://link.springer.com/journal/371/submission-guidelines) 准备数据可用性声明和可编辑源材料，说明代码、trace 与底层数据许可。

[TVCG Replicability Stamp](https://www.computer.org/digital-library/journals/tg/tvcg-replicability-stamp-now-available/) 可作为复现材料的目标，但复现认证不替代科学新颖性和论文评审。

最有价值的下一步是用新版系统与 VLN 专家完成一次真实调试闭环：记录一个有证据的假设，补充所需日志或受控模型运行，检验该假设，再评价系统在这个过程中如何帮助发现和排除解释。这比继续增加缺少证据来源的指标更能强化论文。
